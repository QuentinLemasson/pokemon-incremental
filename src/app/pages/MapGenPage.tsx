import { MapGenView } from '@/features/mapGenView/MapGenView.main';

/**
 * Map Generation Page.
 * Simple page wrapper that delegates to the MapGenView feature component.
 */
export const MapGenPage = () => {
  return <MapGenView />;
};
